{% extends 'layout.html.twig' %}

{% block wrapper %}

    {% if is_granted('ROLE_ADMIN') %}

        <a href="...">ok</a>
        <p>User: {{ app.user }}</p>

    {% endif %}

    <br/>
    Query users: 
    {% set user0 = app.request.server.get('query_user') %}
    {{ app.request.server.get('query_user') }}

    <a href="{{ path('app_secure_route', {'user': 'john', 'pass': 'dely@9'}) }}">Login</a>

{% endblock %}
