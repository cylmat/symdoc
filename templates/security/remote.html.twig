{% extends 'layout.html.twig' %}

{% block wrapper %}

    {% if is_granted('ROLE_ADMIN') %}

        <a href="...">ok</a>
        <p>User: {{ app.user }}</p>

    {% endif %}

    <br/>
    Query users: 
    {{ app.request.server.get('query_user') }}

{% endblock %}
